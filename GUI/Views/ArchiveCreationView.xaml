<UserControl x:Class="GUI.Views.ArchiveCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GUI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ArchiveCreationViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">

        <Border Background="White" CornerRadius="8" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="600" Padding="0"
                BorderBrush="#DDDDDD" BorderThickness="1">

            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="#333" CornerRadius="8,8,0,0" Padding="15">
                    <TextBlock Text="Create New Archive" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="500">
                    <StackPanel Margin="20">

                        <TextBlock Text="1. Items to Archive" FontWeight="Bold" Foreground="#555"/>
                        <Grid Margin="0,5,0,20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Button Content="+ Add Files" Command="{Binding AddFilesCommand}" Padding="10,5" Margin="0,0,5,0"/>
                                <Button Content="+ Add Folder" Command="{Binding AddFolderCommand}" Padding="10,5"/>
                            </StackPanel>

                            <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" Background="White" Height="100" CornerRadius="4">
                                <ListBox ItemsSource="{Binding ItemsToArchive}" BorderThickness="0">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding}" ToolTip="{Binding}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                                                <Button Grid.Column="1" Content="❌" 
                                                Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                                CommandParameter="{Binding}"
                                                Background="Transparent" BorderThickness="0" Foreground="Red" Margin="10,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>
                        </Grid>

                        <TextBlock Text="2. Output Settings" FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding ArchiveName, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                            <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseDestinationCommand}" Margin="5,0,0,0" Padding="5"/>
                        </Grid>
                        <TextBlock Text="{Binding FullOutputPath}" Foreground="Gray" FontSize="11" FontStyle="Italic" Margin="0,-10,0,20"/>


                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="Format" FontWeight="Bold" Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <RadioButton Content=".zip" IsChecked="{Binding IsZip}" GroupName="Fmt" Margin="0,0,15,0"/>
                                    <RadioButton Content=".7z" IsChecked="{Binding Is7z}" GroupName="Fmt"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Compression" FontWeight="Bold" Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding CompressionOptions}" SelectedItem="{Binding SelectedCompression}"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="3. Security (Optional)" FontWeight="Bold" Foreground="#555"/>

                        <TextBox Text="{Binding Password}" Margin="0,5,0,0" Padding="5"/>
                        <TextBlock Text="Leave empty for no encryption" FontSize="10" Foreground="Gray"/>

                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2" BorderBrush="#EEE" BorderThickness="0,1,0,0" Padding="15" Background="#FAFAFA" CornerRadius="0,0,8,8">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Command="{Binding GoBackCommand}" Width="80" Background="Transparent" BorderThickness="0"/>
                        <Button Content="Create Archive" Command="{Binding CreateArchiveCommand}" Width="120" Height="30" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Grid.RowSpan="3" Background="#CCFFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" CornerRadius="8">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar Value="{Binding ProgressValue}" Width="200" Height="10" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding ProgressMessage}"/>
                        <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

            </Grid>
        </Border>
    </Grid>
</UserControl>